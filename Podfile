# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NiceLoginSample' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  pod 'BEMCheckBox'
  pod 'Masonry'
  pod 'SDWebImage'            # 이미지로딩 라이브러리
  pod 'CocoaLumberjack'        # 로그용
  pod 'JSONModel'                # JSON 라이브러리
  pod 'SVProgressHUD'            # HUD 라이브러리
  pod 'M13ProgressSuite'        # 로딩 라이브러리
  pod 'ODRefreshControl'        # 스크롤뷰 리프레시 라이브러리
  pod 'Toast', '~> 3.0'        # 안드로이드 Toast 스타일 라이브러리
  pod 'Fabric'                # Fabric
  pod 'Crashlytics'            # Fabrid의 크래시리틱스 (크래쉬툴)
  pod 'AFNetworking', '~> 3.0'
  # Pods for NiceLoginSample

end
